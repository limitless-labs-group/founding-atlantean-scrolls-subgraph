{
  "name": "atlantis-world-founding-atlantean-scrolls",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ maxschnaider/atlantis-world-founding-atlantean-scrolls",
    "create-local": "graph create --node http://localhost:8020/ maxschnaider/atlantis-world-founding-atlantean-scrolls",
    "remove-local": "graph remove --node http://localhost:8020/ maxschnaider/atlantis-world-founding-atlantean-scrolls",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 maxschnaider/atlantis-world-founding-atlantean-scrolls"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
